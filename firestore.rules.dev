// REGLAS TEMPORALES PARA DESARROLLO - MÁS PERMISIVAS
// ⚠️ SOLO PARA DESARROLLO - NO USAR EN PRODUCCIÓN
// Estas reglas permiten que cualquier usuario autenticado pueda leer/escribir
// Úsalas solo para probar que todo funciona, luego cambia a las reglas de producción

rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Permitir todo a usuarios autenticados (SOLO PARA DESARROLLO)
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}


